<% if @active_storage_files.empty? %>
  <%= render_message do %>
    <% if search_filter_params[:q].present? %>
      <%= Alchemy.t(:no_search_results) %>
    <% else %>
      <%= Alchemy.t(:no_files_in_archive) %>
    <% end %>
  <% end %>
<% else %>
  <ul class="list">
    <li class="list-header">
      <span class="list-primary">
        <%= Alchemy::ActiveStorageFile.human_attribute_name('name') %>
      </span>
      <span class="list-secondary">
        <%= Alchemy::ActiveStorageFile.human_attribute_name('file_type') %>
      </span>
    </li>
    <%= render partial: 'file_to_assign', collection: @active_storage_files %>
  </ul>
  <%= paginate @active_storage_files, theme: 'alchemy', remote: true, hide_per_page_select: true %>
<% end %>
